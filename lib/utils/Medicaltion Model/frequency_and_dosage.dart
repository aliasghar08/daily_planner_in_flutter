// lib/models/medication_enums.dart

enum MedicationFrequency { daily, weekly, monthly, asNeeded, custom }

enum DosageUnit { mg, mcg, ml, tablet, capsule, drop, spray, puff }

enum IntakeStatus { pending, taken, missed, skipped, upcoming }
